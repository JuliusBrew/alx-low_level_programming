#include "main.h"

/**
 *_abs - returns 1 if alphabet is lower case
 * @c: The character to print
 * Return: Always (Success)
 *
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	return (n);

	_putchar('\n');
}
